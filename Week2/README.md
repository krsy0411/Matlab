# 3월 13일 수업 내용(3월 11일에는 대만여행으로 인해 불참)
## 내장함수
* 최대 최소
* 행렬
* 정렬함수
* 함수 이름 설정
* 행렬 이론
* 특수 행렬
* 배열 원소 개별 접근 | index
* 배열 원소 그룹 접근 | :


## 행렬 이론
두 행렬이 같다?
- 행렬이 크기가 같다
- 행렬의 각 성분의 값이 동일하다(a11 = b11, a12 = b12, ...)
<br/>

### 행렬끼리의 곱
Aij(iXj크기) 와 Brc(rXc크기)인 경우에 서로의 곱 C = AB가 되기 위해서의 필요충분조건은 **j=r**입니다.<br/>
**행렬과 벡터의 곱도 마찬가지**<br/>
### 행렬의 곱셈에서 교환법칙은 성립하지 않는다
AB != BA<br/>
### 선형 연립 방정식
선형 연립 방정식은 행렬로 만들 수 있습니다<br/>
### 전치행렬
행렬의 행들을 열로(or 열들을 행들로) 나타냄<br/>
### 단위행렬
* 주 대각선상의 성분이 모두 1인 **스칼라 행렬**
* 단위행렬은 **정사각행렬**
* AI = IA = A
### 행렬의 역행렬
* AB = BA = I : A행렬에 B를 곱해서 단위행렬이 나온다 -> B는 A의 역행렬
* A*A-1 = A-1*A = I
* A*X = b -> A-1*A*X = A-1*B -> X = A-1*B
* 모든 행렬이 역행렬을 갖진 않음
* 역행렬을 가지려면 **det != 0** 이어야함
* 선형 방정식에서 역행렬이 존재하면 해를 쉽게 구함
<br/>



